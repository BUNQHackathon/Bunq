# LaunchLens data-prep scripts — separate from sidecar's pyproject.toml
requests>=2.31
pypdf>=4.0
openpyxl>=3.1
boto3>=1.34
pytest>=8.0
moto[dynamodb,s3]>=5.0
# pandas intentionally omitted — OFAC CSV joining done with stdlib csv module
