{
  "name": "extract_obligations",
  "description": "Extract legal obligations from a regulation text chunk. Each obligation must be a concrete, testable duty/prohibition/permission for a subject. Use DDL operators [O], [F], [P]. If you cannot ground the obligation in the chunk text, do not emit it. Call this tool exactly once with the structured list of obligations you extracted.",
  "input_schema": {
    "type": "object",
    "properties": {
      "obligations": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "reasoning": {"type": "string", "description": "Before extracting, state in one sentence WHY this is a concrete legal duty grounded in the quoted text. If the text states no testable duty, do not emit this item."},
            "source_text_snippet": {"type": "string", "description": "Quote from input that grounds this obligation"},
            "deontic": {"type": "string", "enum": ["[O]", "[F]", "[P]"]},
            "subject": {"type": "string"},
            "action": {"type": "string"},
            "conditions": {"type": "array", "items": {"type": "string"}},
            "risk_category": {"type": "string"},
            "severity": {"type": "string", "enum": ["low", "medium", "high", "critical"]},
            "extraction_confidence": {"type": "number"},
            "article": {"type": "string", "description": "Article number from the regulation text, e.g. 'Article 4' or '4(2)'. Empty string if not identifiable."},
            "section": {"type": "string", "description": "Section heading or chapter name if visible, else empty string."},
            "paragraph": {"type": "integer", "description": "Paragraph number within the article if identifiable, else 0."}
          },
          "required": ["reasoning", "source_text_snippet", "deontic", "subject", "action", "extraction_confidence"]
        }
      }
    },
    "required": ["obligations"]
  }
}
