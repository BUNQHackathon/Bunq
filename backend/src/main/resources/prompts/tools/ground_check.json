{
  "name": "ground_check",
  "description": "Verify that a cited passage actually exists verbatim (or with negligible paraphrase) inside a retrieved chunk. Return true only if the citation is grounded.",
  "input_schema": {
    "type": "object",
    "properties": {
      "citation_text": {"type": "string"},
      "retrieved_chunk_text": {"type": "string"}
    }
  },
}
