{
  "name": "extract_counterparties_from_brief",
  "description": "Extract mentioned counterparty entities (companies, individuals, organizations) from a feature brief. Include country and entity type if deducible from the text. Call this tool exactly once with the structured list of counterparties.",
  "input_schema": {
    "type": "object",
    "properties": {
      "counterparties": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "name": {"type": "string"},
            "country": {"type": "string"},
            "type": {"type": "string", "enum": ["individual", "company", "organization", "government", "unknown"]},
            "confidence": {"type": "number"}
          },
          "required": ["name"]
        }
      }
    },
    "required": ["counterparties"]
  }
}
